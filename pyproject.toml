[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "moat-ovha"
version = "0.1.0"
description = "Phase-1 prototype for Operator-Valued Hyper-Attention."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
include = ["moat_ovha*"]
